#include<iostream>
using namespace std;
int add(int n)
{
    int i,a=0;
    for(i=1;i<=n;i++)
    {
        a=a+i;
        
    }cout<<"Sum of n numbers:"<<endl;
    cout<<a;
    return 0;
}
int main()
{
    int n;
    cin>>n;
    add(n);
}
